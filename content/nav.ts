import type { NavItem } from './types'

// The site is a single scroll journey, so nav stays light: jump to contact,
// grab the résumé, flip the theme.
export const navItems: NavItem[] = [{ label: 'Let’s get to work', href: '/#contact' }]
